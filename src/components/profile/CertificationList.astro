---
import { formatCount, formatYearMonth } from '../../lib/format';
import { toCertificationDetail } from '../../lib/url';

interface Certification {
  slug: string;
  organizer: string;
  name: string;
  result: string;
  acquiredDate: string;
  expiryDate: string | null;
}

interface Props {
  certifications: Certification[];
}

const { certifications } = Astro.props;
---

<section class="surface-card stack" data-reveal>
  <p class="muted">登録資格: {formatCount(certifications.length)}</p>
  <ul class="profile-list">
    {certifications.map((certification) => (
      <li class="profile-card">
        <p class="blog-meta">{certification.organizer}</p>
        <strong>
          <a href={toCertificationDetail(certification.slug)}>{certification.name}</a>
        </strong>
        <p>{formatYearMonth(certification.acquiredDate)} {certification.result}</p>
      </li>
    ))}
  </ul>
</section>
