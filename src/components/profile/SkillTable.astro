---
import { formatCount, formatExperienceYears, formatLevel } from '../../lib/format';

interface SkillItem {
  name: string;
  years: number;
  level: number;
}

interface SkillCategory {
  category: string;
  items: SkillItem[];
}

interface Props {
  skills: SkillCategory[];
}

const { skills } = Astro.props;
const skillSummary = skills
  .map((skill) => `${skill.category}: ${formatCount(skill.items.length)}`)
  .join(' / ');
---

<section class="surface-card stack" data-reveal>
  <p class="muted">{skillSummary}</p>
  {skills.map((skill) => (
    <section class="stack">
      <h3>{skill.category}</h3>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>スキル</th>
              <th>経験年数</th>
              <th>レベル</th>
            </tr>
          </thead>
          <tbody>
            {skill.items.map((item) => (
              <tr>
                <td>{item.name}</td>
                <td>{formatExperienceYears(item.years)}</td>
                <td>{formatLevel(item.level)}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>
  ))}
</section>
